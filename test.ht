# Generated from OpenAPI spec

@get_pets_query_limit = {{$get_pets_query_limit}}
@get_pets_query_page = {{$get_pets_query_page}}
@get_pets_query_status = {{$get_pets_query_status}}
@get_pets_petId_path_petId = {{$get_pets_petId_path_petId}}
@put_pets_petId_path_petId = {{$put_pets_petId_path_petId}}
@delete_pets_petId_path_petId = {{$delete_pets_petId_path_petId}}
@post_pets_petId_upload_path_petId = {{$post_pets_petId_upload_path_petId}}
@get_pets_filter_query_filters = {{$get_pets_filter_query_filters}}

### #main
GET {{baseUrl}}/

### #login
# Вход в систему
POST https://api.petstore.com/v1/auth/login
Content-Type: application/json

{
    "email": "admin@petstore.com",
    "password": "secret123"
}

> {%
$statusMessages = array (
  200 => 'Успешный вход',
  401 => 'Неверные учетные данные',
);

if (isset($statusMessages[$response->status_code])) {
    $output->append($statusMessages[$response->status_code]);
} elseif ($response->status_code >= 500) {
    $output->append('Internal server error: ' . $response->status_code);
} elseif ($response->status_code >= 400) {
    $output->append('Client error: ' . $response->status_code);
}
%}

### #me
# Информация о текущем пользователе
GET https://api.petstore.com/v1/auth/me

> {%
$statusMessages = array (
  200 => 'OK',
  401 => 'Status 401',
);

if (isset($statusMessages[$response->status_code])) {
    $output->append($statusMessages[$response->status_code]);
} elseif ($response->status_code >= 500) {
    $output->append('Internal server error: ' . $response->status_code);
} elseif ($response->status_code >= 400) {
    $output->append('Client error: ' . $response->status_code);
}
%}

### #listPets
# Список питомцев
GET https://api.petstore.com/v1/pets
limit={{get_pets_query_limit}}
page={{get_pets_query_page}}
status={{get_pets_query_status}}

> {%
$statusMessages = array (
  200 => 'OK',
);

if (isset($statusMessages[$response->status_code])) {
    $output->append($statusMessages[$response->status_code]);
} elseif ($response->status_code >= 500) {
    $output->append('Internal server error: ' . $response->status_code);
} elseif ($response->status_code >= 400) {
    $output->append('Client error: ' . $response->status_code);
}
%}

### #createPet
# Создать питомца
POST https://api.petstore.com/v1/pets
Content-Type: application/json

{
    "id": 1,
    "name": "\u0411\u0430\u0440\u0441\u0438\u043a",
    "category": {},
    "tags": [],
    "status": "available",
    "age": 3
}

> {%
$statusMessages = array (
  201 => 'Создано',
  422 => 'Ошибка валидации',
);

if (isset($statusMessages[$response->status_code])) {
    $output->append($statusMessages[$response->status_code]);
} elseif ($response->status_code >= 500) {
    $output->append('Internal server error: ' . $response->status_code);
} elseif ($response->status_code >= 400) {
    $output->append('Client error: ' . $response->status_code);
}
%}

### #getPet
# Получить питомца
GET https://api.petstore.com/v1/pets/{{get_pets_petId_path_petId}}

> {%
$statusMessages = array (
  200 => 'OK',
  404 => 'Status 404',
);

if (isset($statusMessages[$response->status_code])) {
    $output->append($statusMessages[$response->status_code]);
} elseif ($response->status_code >= 500) {
    $output->append('Internal server error: ' . $response->status_code);
} elseif ($response->status_code >= 400) {
    $output->append('Client error: ' . $response->status_code);
}
%}

### #updatePet
# Обновить питомца
PUT https://api.petstore.com/v1/pets/{{put_pets_petId_path_petId}}
Content-Type: application/json

{
    "id": 1,
    "name": "\u0411\u0430\u0440\u0441\u0438\u043a",
    "category": {},
    "tags": [],
    "status": "available",
    "age": 3
}

> {%
$statusMessages = array (
  200 => 'Status 200',
  404 => 'Status 404',
);

if (isset($statusMessages[$response->status_code])) {
    $output->append($statusMessages[$response->status_code]);
} elseif ($response->status_code >= 500) {
    $output->append('Internal server error: ' . $response->status_code);
} elseif ($response->status_code >= 400) {
    $output->append('Client error: ' . $response->status_code);
}
%}

### #deletePet
# Удалить питомца
DELETE https://api.petstore.com/v1/pets/{{delete_pets_petId_path_petId}}

> {%
$statusMessages = array (
  204 => 'Удалено без контента',
  404 => 'Status 404',
);

if (isset($statusMessages[$response->status_code])) {
    $output->append($statusMessages[$response->status_code]);
} elseif ($response->status_code >= 500) {
    $output->append('Internal server error: ' . $response->status_code);
} elseif ($response->status_code >= 400) {
    $output->append('Client error: ' . $response->status_code);
}
%}

### #uploadPetPhoto
# Загрузить фото
POST https://api.petstore.com/v1/pets/{{post_pets_petId_upload_path_petId}}/upload

> {%
$statusMessages = array (
  201 => 'Фото загружено',
);

if (isset($statusMessages[$response->status_code])) {
    $output->append($statusMessages[$response->status_code]);
} elseif ($response->status_code >= 500) {
    $output->append('Internal server error: ' . $response->status_code);
} elseif ($response->status_code >= 400) {
    $output->append('Client error: ' . $response->status_code);
}
%}

### #filterPets
# Сложный поиск с фильтрами
GET https://api.petstore.com/v1/pets/filter
filters={{get_pets_filter_query_filters}}

> {%
$statusMessages = array (
  200 => 'OK',
);

if (isset($statusMessages[$response->status_code])) {
    $output->append($statusMessages[$response->status_code]);
} elseif ($response->status_code >= 500) {
    $output->append('Internal server error: ' . $response->status_code);
} elseif ($response->status_code >= 400) {
    $output->append('Client error: ' . $response->status_code);
}
%}
